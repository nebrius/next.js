# Next.js Linting Performance Comparison

This repository is a fork of the Next.js project used to compare the performance of different linting configurations. To run the benchmark:

```bash
pnpm install
node --experimental-strip-types compare.ts
```
